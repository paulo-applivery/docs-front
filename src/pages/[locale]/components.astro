---
/**
 * Component Showcase Page
 * docs.applivery.com/{locale}/components
 *
 * Reference page showing all available documentation components
 * with usage instructions, syntax code, and live desktop/mobile previews.
 */

import BaseLayout from '../../layouts/BaseLayout.astro';
import { processMarkdown } from '../../lib/markdown';
import { locales } from '../../lib/i18n';

export async function getStaticPaths() {
  return locales.map(locale => ({
    params: { locale },
  }));
}

const { locale } = Astro.params;

// ── Helper: escape HTML for displaying raw syntax in <code> blocks ──
function esc(s: string): string {
  return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}

// ── Define each component's data ────────────────────────────────────
// Each entry: { name, description, syntax (raw text), markdown (to render) }

const components = [
  {
    name: 'Headings',
    description: 'Use headings to organize content hierarchy. In the CMS, H1 is automatically converted to H2.',
    syntax: `# Heading 1 (renders as H2)

## Heading 2

### Heading 3

#### Heading 4`,
    markdown: `# Heading 1 (renders as H2)

## Heading 2

### Heading 3

#### Heading 4`,
  },
  {
    name: 'Text Formatting',
    description: 'Standard markdown formatting for emphasis, strikethrough, and inline code.',
    syntax: `This is **bold text** and this is *italic text*.

This is ***bold and italic*** together.

This is ~~strikethrough~~ text.

This is \`inline code\` within a sentence.`,
    markdown: `This is **bold text** and this is *italic text*.

This is ***bold and italic*** together.

This is ~~strikethrough~~ text.

This is \`inline code\` within a sentence.`,
  },
  {
    name: 'Links',
    description: 'Standard markdown links. External links open in a new tab automatically.',
    syntax: `[Regular link](https://applivery.com)

[External link](https://github.com)`,
    markdown: `This is a [regular link](https://applivery.com) and this is an [external link](https://github.com).`,
  },
  {
    name: 'Lists',
    description: 'Unordered and ordered lists with nesting support.',
    syntax: `- First item
- Second item
  - Nested item A
  - Nested item B
- Third item

1. First step
2. Second step
   1. Sub-step A
   2. Sub-step B
3. Third step`,
    markdown: `- First item
- Second item
  - Nested item A
  - Nested item B
- Third item

1. First step
2. Second step
   1. Sub-step A
   2. Sub-step B
3. Third step`,
  },
  {
    name: 'Blockquote',
    description: 'Use blockquotes for citing sources or highlighting important passages.',
    syntax: `> This is a blockquote with **bold** and \`code\`.
>
> It can span multiple paragraphs.`,
    markdown: `> This is a blockquote with **bold** and \`code\`.
>
> It can span multiple paragraphs.`,
  },
  {
    name: 'Tables',
    description: 'Standard markdown tables for structured data.',
    syntax: `| Property | Type   | Required | Description        |
|----------|--------|----------|--------------------|
| name     | string | Yes      | The display name   |
| email    | string | Yes      | Contact email      |
| role     | enum   | No       | User role          |`,
    markdown: `| Property | Type | Required | Description |
|----------|------|----------|-------------|
| name | string | Yes | The display name |
| email | string | Yes | Contact email |
| role | enum | No | User role (admin, editor, viewer) |
| active | boolean | No | Whether the user is active |`,
  },
  {
    name: 'Code Block',
    description: 'Fenced code blocks with optional language for syntax highlighting.',
    syntax: '```\nPlain code block without highlighting.\n```\n\n```javascript\nconst greeting = "Hello, World!";\nconsole.log(greeting);\n```',
    markdown: `\`\`\`
Plain code block without highlighting.
\`\`\`

\`\`\`javascript
const greeting = "Hello, World!";
console.log(greeting);
\`\`\``,
  },
  {
    name: 'Code Block with Filename',
    description: 'Add a title attribute to display a filename header with a copy button.',
    syntax: '```typescript title="src/config.ts"\ninterface AppConfig {\n  apiUrl: string;\n  debug: boolean;\n}\n```',
    markdown: `\`\`\`typescript title="src/config.ts"
interface AppConfig {
  apiUrl: string;
  debug: boolean;
  version: string;
}

export const config: AppConfig = {
  apiUrl: "https://api.applivery.com",
  debug: false,
  version: "1.0.0",
};
\`\`\``,
  },
  {
    name: 'Callouts',
    description: 'Admonition blocks to highlight important information. Available types: note, info, tip, success, important, warning, danger, caution.',
    syntax: `:::note
Highlights information that users should take into account.
:::

:::tip
Optional information to help a user be more successful.
:::

:::important
Crucial information necessary for users to succeed.
:::

:::warning
Critical content demanding immediate user attention.
:::

:::caution
Negative potential consequences of an action.
:::`,
    markdown: `:::note
Highlights information that users should take into account, even when skimming.
:::

:::tip
Optional information to help a user be more successful.
:::

:::success
Operation completed successfully. Your changes have been saved.
:::

:::important
Crucial information necessary for users to succeed.
:::

:::warning
Critical content demanding immediate user attention due to potential risks.
:::

:::caution
Negative potential consequences of an action.
:::`,
  },
  {
    name: 'Accordion',
    description: 'Collapsible sections for content that can be expanded on demand.',
    syntax: `<Accordion title="Click to expand">
Your content here with **markdown** support.

- List items work too
- Another item
</Accordion>`,
    markdown: `<Accordion title="What is Applivery?">
Applivery is a platform for **mobile device management** and **app distribution**. It supports Android, iOS, and Windows devices.

You can use it to:
- Manage device policies
- Distribute apps
- Monitor compliance
</Accordion>

<Accordion title="How do I get started?">
Sign up for a free account at [applivery.com](https://applivery.com) and follow the onboarding guide.
</Accordion>`,
  },
  {
    name: 'Tabs',
    description: 'Tabbed content for showing alternatives like code in multiple languages.',
    syntax: `<Tabs>
<Tab title="JavaScript">
\\\`\\\`\\\`javascript
const data = await fetch("/api");
\\\`\\\`\\\`
</Tab>
<Tab title="Python">
\\\`\\\`\\\`python
response = requests.get("/api")
\\\`\\\`\\\`
</Tab>
</Tabs>`,
    markdown: `<Tabs>
<Tab title="JavaScript">
\`\`\`javascript
const response = await fetch("https://api.applivery.com/v1/devices");
const data = await response.json();
console.log(data);
\`\`\`
</Tab>
<Tab title="Python">
\`\`\`python
import requests

response = requests.get("https://api.applivery.com/v1/devices")
data = response.json()
print(data)
\`\`\`
</Tab>
<Tab title="cURL">
\`\`\`bash
curl -X GET "https://api.applivery.com/v1/devices" \\
  -H "Authorization: Bearer YOUR_API_KEY"
\`\`\`
</Tab>
</Tabs>`,
  },
  {
    name: 'Steps',
    description: 'Numbered step-by-step guides with titles and content for each step.',
    syntax: `<Steps>
<Step title="Create an account">
Go to applivery.com and sign up.
</Step>
<Step title="Install the SDK">
\\\`\\\`\\\`bash
npm install @applivery/sdk
\\\`\\\`\\\`
</Step>
</Steps>`,
    markdown: `<Steps>
<Step title="Create an account">
Go to [applivery.com](https://applivery.com) and sign up for a free account. You will need a valid email address.
</Step>
<Step title="Install the SDK">
Install the Applivery SDK in your project using your preferred package manager.

\`\`\`bash
npm install @applivery/sdk
\`\`\`
</Step>
<Step title="Configure your app">
Initialize the SDK with your API key and configure the settings for your application.
</Step>
<Step title="Deploy">
Build and deploy your application. The SDK will handle updates automatically.
</Step>
</Steps>`,
  },
  {
    name: 'Cards',
    description: 'Card components for linking to related pages. Use CardGroup to arrange in columns (1-4). Available icons: rocket, smartphone, zap, code, shield, users, file-text, settings, book, globe, database, lock, star, heart, bell, mail, terminal, and more.',
    syntax: `<Card title="Getting Started" icon="rocket" href="/en/guides">
Learn the basics of the platform.
</Card>

<CardGroup cols={2}>
<Card title="Devices" icon="smartphone" href="/en/devices">
Manage all your devices.
</Card>
<Card title="Apps" icon="zap" href="/en/apps">
Distribute apps to your team.
</Card>
</CardGroup>

<CardGroup cols={3}>
<Card title="API" icon="code">
Full REST API docs.
</Card>
<Card title="Security" icon="shield">
Security best practices.
</Card>
<Card title="Users" icon="users">
Manage user access.
</Card>
</CardGroup>`,
    markdown: `<Card title="Getting Started" icon="rocket" href="/en/guides/getting-started">
Learn the basics of the platform and set up your first project.
</Card>

<CardGroup cols={2}>
<Card title="Device Management" icon="smartphone" href="/en/device-management">
Manage Android, iOS, and Windows devices from a single console.
</Card>
<Card title="App Distribution" icon="zap" href="/en/app-distribution">
Distribute apps to your team with over-the-air updates.
</Card>
</CardGroup>

<CardGroup cols={3}>
<Card title="API Reference" icon="code">
Full REST API documentation with examples.
</Card>
<Card title="Security" icon="shield">
Security best practices and compliance.
</Card>
<Card title="Users & Teams" icon="users">
Manage user access and team permissions.
</Card>
</CardGroup>`,
  },
  {
    name: 'API Endpoint',
    description: 'Display API endpoint badges with method and path. Used in API reference pages.',
    syntax: `<APIEndpoint method="GET" path="/v1/devices">
Returns a list of all managed devices.
</APIEndpoint>

<APIEndpoint method="POST" path="/v1/devices/{id}/commands">
Send a command to a specific device.
</APIEndpoint>

<APIEndpoint method="DELETE" path="/v1/devices/{id}">
Remove a device from the organization.
</APIEndpoint>`,
    markdown: `<APIEndpoint method="GET" path="/v1/devices">
Returns a list of all managed devices in the organization.
</APIEndpoint>

<APIEndpoint method="POST" path="/v1/devices/{deviceId}/commands">
Send a command to a specific device.
</APIEndpoint>

<APIEndpoint method="DELETE" path="/v1/devices/{deviceId}">
Remove a device from the organization.
</APIEndpoint>`,
  },
  {
    name: 'Browser Mockup',
    description: 'A browser frame wrapper for previewing dashboard UIs or web content. Supports light, dark, and auto themes.',
    syntax: `<BrowserMockup url="https://dashboard.applivery.com" theme="auto">

### Dashboard Preview

Content rendered inside a browser chrome frame.

| Status  | Count |
|---------|-------|
| Online  | 142   |
| Offline | 23    |

</BrowserMockup>`,
    markdown: `<BrowserMockup url="https://dashboard.applivery.com" theme="auto">

### Welcome to the Dashboard

This is a preview of what the Applivery dashboard looks like. You can manage all your devices, apps, and policies from here.

| Status | Count |
|--------|-------|
| Online | 142 |
| Offline | 23 |
| Pending | 7 |

</BrowserMockup>`,
  },
];

// ── Process each component's markdown into HTML ─────────────────────
const processed = components.map(comp => ({
  ...comp,
  html: processMarkdown(comp.markdown),
  escapedSyntax: esc(comp.syntax),
}));
---

<BaseLayout
  title="Component Showcase"
  description="All available documentation components with usage instructions, syntax, and live previews"
>
  <main class="showcase-page">
    <a href={`/${locale}`} class="showcase-back-link">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="19" y1="12" x2="5" y2="12"></line>
        <polyline points="12 19 5 12 12 5"></polyline>
      </svg>
      Back to Docs
    </a>

    <header class="showcase-header">
      <h1 class="showcase-title">Component Showcase</h1>
      <p class="showcase-description">Reference for all available documentation components. Each section shows usage instructions, the CMS syntax, and a live preview with light/dark mode toggle.</p>
    </header>

    {processed.map((comp) => (
      <section class="comp-section">
        <div class="comp-heading">
          <h2 class="comp-name">{comp.name}</h2>
          <p class="comp-desc">{comp.description}</p>
        </div>

        <div class="comp-code">
          <div class="comp-code-header">
            <span class="comp-code-label">
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
              Syntax
            </span>
            <button class="comp-copy-btn" data-comp-copy>
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
              <span>Copy</span>
            </button>
          </div>
          <pre class="comp-code-block"><code set:html={comp.escapedSyntax} /></pre>
        </div>

        <div class="comp-preview">
          <div class="comp-preview-frame" data-preview-frame>
            <div class="comp-preview-bar">
              <span class="comp-preview-label">Preview</span>
              <button class="comp-theme-toggle" data-theme-toggle title="Toggle light/dark mode">
                <svg class="comp-theme-icon-light" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"/><line x1="12" y1="1" x2="12" y2="3"/><line x1="12" y1="21" x2="12" y2="23"/><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/><line x1="1" y1="12" x2="3" y2="12"/><line x1="21" y1="12" x2="23" y2="12"/><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/></svg>
                <svg class="comp-theme-icon-dark" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
                <span class="comp-theme-text">Light</span>
              </button>
            </div>
            <div class="comp-preview-content article-content">
              <Fragment set:html={comp.html} />
            </div>
          </div>
        </div>
      </section>
    ))}
  </main>

  <script is:inline>
    function initShowcaseInteractions() {
      // Accordion toggles
      document.querySelectorAll('[data-accordion-trigger]').forEach(function(trigger) {
        if (trigger.dataset.accordionBound) return;
        trigger.dataset.accordionBound = 'true';
        trigger.addEventListener('click', function() {
          var accordion = trigger.closest('[data-accordion]');
          if (!accordion) return;
          var isOpen = accordion.classList.contains('open');
          accordion.classList.toggle('open', !isOpen);
          trigger.setAttribute('aria-expanded', (!isOpen).toString());
        });
      });

      // Tab switching
      document.querySelectorAll('[data-tabs]').forEach(function(tabGroup) {
        if (tabGroup.dataset.tabsBound) return;
        tabGroup.dataset.tabsBound = 'true';
        var buttons = tabGroup.querySelectorAll('[data-tab-trigger]');
        var panels = tabGroup.querySelectorAll('[data-tab-panel]');
        buttons.forEach(function(btn) {
          btn.addEventListener('click', function() {
            var idx = btn.dataset.tabIndex;
            buttons.forEach(function(b) { b.classList.remove('active'); });
            panels.forEach(function(p) {
              if (p.dataset.tabIndex === idx) {
                p.classList.add('active');
              } else {
                p.classList.remove('active');
              }
            });
            btn.classList.add('active');
          });
        });
      });

      // Code copy buttons (for rendered code blocks inside previews)
      document.querySelectorAll('.comp-preview-content pre').forEach(function(pre) {
        if (pre.closest('.doc-code-block')) return;
        if (pre.querySelector('.code-copy-btn')) return;
        var wrapper = document.createElement('div');
        wrapper.className = 'code-block-wrapper';
        pre.parentNode.insertBefore(wrapper, pre);
        wrapper.appendChild(pre);
        var btn = document.createElement('button');
        btn.className = 'code-copy-btn';
        btn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>';
        btn.title = 'Copy code';
        wrapper.appendChild(btn);
      });

      // Copy handler for rendered code blocks + titled code blocks
      document.querySelectorAll('.doc-code-copy, .code-copy-btn').forEach(function(btn) {
        if (btn.dataset.copyBound) return;
        btn.dataset.copyBound = 'true';
        btn.addEventListener('click', async function() {
          var container = btn.closest('.doc-code-block') || btn.closest('.code-block-wrapper');
          var code = container ? container.querySelector('code') : null;
          if (!code) return;
          try {
            await navigator.clipboard.writeText(code.textContent || '');
            var isTitled = btn.classList.contains('doc-code-copy');
            if (isTitled) {
              btn.classList.add('copied');
              var span = btn.querySelector('span');
              if (span) span.textContent = 'Copied!';
              setTimeout(function() { btn.classList.remove('copied'); if (span) span.textContent = 'Copy'; }, 2000);
            } else {
              btn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>';
              btn.classList.add('copied');
              setTimeout(function() { btn.innerHTML = '<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>'; btn.classList.remove('copied'); }, 2000);
            }
          } catch(e) {}
        });
      });

      // Syntax copy buttons
      document.querySelectorAll('[data-comp-copy]').forEach(function(btn) {
        if (btn.dataset.compCopyBound) return;
        btn.dataset.compCopyBound = 'true';
        btn.addEventListener('click', async function() {
          var codeBlock = btn.closest('.comp-code');
          var code = codeBlock ? codeBlock.querySelector('code') : null;
          if (!code) return;
          try {
            await navigator.clipboard.writeText(code.textContent || '');
            var span = btn.querySelector('span');
            if (span) span.textContent = 'Copied!';
            btn.classList.add('copied');
            setTimeout(function() { if (span) span.textContent = 'Copy'; btn.classList.remove('copied'); }, 2000);
          } catch(e) {}
        });
      });

      // Preview theme toggles
      document.querySelectorAll('[data-theme-toggle]').forEach(function(btn) {
        if (btn.dataset.themeBound) return;
        btn.dataset.themeBound = 'true';
        btn.addEventListener('click', function() {
          var frame = btn.closest('[data-preview-frame]');
          if (!frame) return;
          var isDark = frame.classList.toggle('dark');
          var text = btn.querySelector('.comp-theme-text');
          if (text) text.textContent = isDark ? 'Dark' : 'Light';
        });
      });

      // Collapsible body params
      document.querySelectorAll('[data-bp-trigger]').forEach(function(trigger) {
        if (trigger.dataset.bpBound) return;
        trigger.dataset.bpBound = 'true';
        trigger.addEventListener('click', function() {
          var row = trigger.closest('[data-bp-collapsible]');
          if (!row) return;
          row.classList.toggle('open');
        });
      });
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initShowcaseInteractions);
    } else {
      initShowcaseInteractions();
    }
    document.addEventListener('astro:after-swap', initShowcaseInteractions);
  </script>
</BaseLayout>

<style>
  .showcase-page {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem 1.5rem 4rem;
  }

  .showcase-back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-text-secondary);
    text-decoration: none;
    font-size: 0.875rem;
    margin-bottom: 1.5rem;
    transition: color 0.15s;
  }

  .showcase-back-link:hover {
    color: var(--color-primary);
  }

  .showcase-header {
    margin-bottom: 3rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-border);
  }

  .showcase-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 0.5rem;
    font-family: var(--font-heading);
  }

  .showcase-description {
    color: var(--color-text-secondary);
    font-size: 1.0625rem;
    margin: 0;
    line-height: 1.6;
  }

  /* ── Component Section ───────────────────────────────── */

  .comp-section {
    padding: 2.5rem 0;
    border-bottom: 1px solid var(--color-border);
  }

  .comp-section:last-child {
    border-bottom: none;
  }

  .comp-heading {
    margin-bottom: 1.5rem;
  }

  .comp-name {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 0.5rem;
    font-family: var(--font-heading);
  }

  .comp-desc {
    color: var(--color-text-secondary);
    font-size: 0.9375rem;
    margin: 0;
    line-height: 1.6;
    max-width: 720px;
  }

  /* ── Syntax Code Block ───────────────────────────────── */

  .comp-code {
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    overflow: hidden;
    margin-bottom: 1.5rem;
  }

  .comp-code-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.5rem 1rem;
    background: var(--color-background-tertiary);
    border-bottom: 1px solid var(--color-border);
  }

  .comp-code-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .comp-copy-btn {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.25rem 0.5rem;
    font-size: 0.75rem;
    color: var(--color-text-tertiary);
    background: transparent;
    border: none;
    border-radius: 0.25rem;
    cursor: pointer;
    transition: all 0.15s;
  }

  .comp-copy-btn:hover {
    color: var(--color-text-primary);
    background: var(--color-background-secondary);
  }

  .comp-copy-btn.copied {
    color: var(--color-primary);
  }

  .comp-code-block {
    margin: 0;
    padding: 1rem;
    background: var(--color-code-block-bg);
    color: var(--color-code-block-text);
    overflow-x: auto;
    font-family: 'JetBrains Mono', ui-monospace, monospace;
    font-size: 0.8125rem;
    line-height: 1.7;
  }

  .comp-code-block code {
    font-family: inherit;
    color: inherit;
    background: transparent;
    padding: 0;
    border: none;
  }

  /* ── Preview Area ────────────────────────────────────── */

  .comp-preview {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .comp-preview-frame {
    border: 1px solid var(--color-border);
    border-radius: 0.5rem;
    overflow: hidden;
    background: var(--color-background);
  }

  .comp-preview-frame {
    flex: 1;
    min-width: 0;
    transition: background-color 0.2s, color 0.2s;
  }

  /* When the frame has .dark, it activates dark CSS variables */
  .comp-preview-frame.dark {
    background: var(--color-background);
  }

  .comp-preview-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.5rem 1rem;
    background: var(--color-background-tertiary);
    border-bottom: 1px solid var(--color-border);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .comp-preview-label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .comp-theme-toggle {
    display: flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.25rem 0.625rem;
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    color: var(--color-text-tertiary);
    background: var(--color-background-secondary);
    border: 1px solid var(--color-border);
    border-radius: 9999px;
    cursor: pointer;
    transition: all 0.15s;
  }

  .comp-theme-toggle:hover {
    color: var(--color-text-primary);
    border-color: var(--color-text-tertiary);
  }

  /* Show sun icon by default (light mode), hide moon */
  .comp-theme-icon-light { display: block; }
  .comp-theme-icon-dark { display: none; }

  /* When frame is dark, show moon icon, hide sun */
  .comp-preview-frame.dark .comp-theme-icon-light { display: none; }
  .comp-preview-frame.dark .comp-theme-icon-dark { display: block; }

  .comp-preview-content {
    padding: 1.5rem;
    overflow: hidden;
  }

  /* Remove top margin from first element and bottom margin from last in previews */
  .comp-preview-content > :first-child,
  .comp-preview-content :first-child {
    margin-top: 0;
  }

  .comp-preview-content > :last-child,
  .comp-preview-content :last-child {
    margin-bottom: 0;
  }

  /* ── Responsive ──────────────────────────────────────── */

  @media (max-width: 900px) {
    .comp-preview {
      flex-direction: column;
    }
  }

  @media (max-width: 640px) {
    .showcase-page {
      padding: 1.5rem 1rem 3rem;
    }

    .showcase-title {
      font-size: 1.5rem;
    }

    .comp-name {
      font-size: 1.25rem;
    }
  }
</style>
